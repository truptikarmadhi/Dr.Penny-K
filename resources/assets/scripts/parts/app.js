export class App {
  init() {
    // $(window).on('scroll', function () {
    //   const isMobile = window.innerWidth <= 991.98; // You can adjust the breakpoint
    //   if (!isMobile) return; // Exit if not mobile/tablet

    //   const scrollTop = $(window).scrollTop();
    //   const heroHeight = $('.hero-section').outerHeight();

    //   if (scrollTop > heroHeight - 100) {
    //     $('.hero-section').addClass('un-sticky');
    //   } else {
    //     $('.hero-section').removeClass('un-sticky');
    //   }
    // });
  }
}
